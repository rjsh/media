<dec f='include/vpx/vpx_encoder.h' l='416' type='unsigned int'/>
<offset>352</offset>
<doc f='include/vpx/vpx_encoder.h' l='404'>/*!\brief Allow lagged encoding
     *
     * If set, this value allows the encoder to consume a number of input
     * frames before producing output frames. This allows the encoder to
     * base decisions for the current frame on future frames. This does
     * increase the latency of the encoding pipeline, so it is not appropriate
     * in all situations (ex: realtime encoding).
     *
     * Note that this is a maximum value -- the encoder may produce frames
     * sooner than the given limit. Set this value to 0 to disable this
     * feature.
     */</doc>
<use f='ffmpeg/libavcodec/libvpxenc.c' l='176' u='r' c='dump_enc_cfg'/>
<use f='ffmpeg/libavcodec/libvpxenc.c' l='459' u='w' c='vpx_init'/>
<use f='ffmpeg/libavcodec/libvpxenc.c' l='564' u='w' c='vpx_init'/>
