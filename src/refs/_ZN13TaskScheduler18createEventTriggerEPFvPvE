<dec f='include/UsageEnvironment/UsageEnvironment.hh' l='144' type='EventTriggerId TaskScheduler::createEventTrigger(TaskFunc * eventHandlerProc)'/>
<doc f='include/UsageEnvironment/UsageEnvironment.hh' l='140'>// Causes further execution to take place within the event loop.
      // Delayed tasks, background I/O handling, and other events are handled, sequentially (as a single thread of control).
      // (If &quot;watchVariable&quot; is not NULL, then we return from this routine when *watchVariable != 0)</doc>
