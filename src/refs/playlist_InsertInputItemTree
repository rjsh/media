<dec f='vlc/src/playlist/playlist_internal.h' l='130' type='int playlist_InsertInputItemTree(playlist_t * , playlist_item_t * , input_item_node_t * , int , _Bool )'/>
<use f='vlc/src/playlist/item.c' l='123' u='c' c='input_item_add_subitem_tree'/>
<def f='vlc/src/playlist/item.c' l='564' type='int playlist_InsertInputItemTree(playlist_t * p_playlist, playlist_item_t * p_parent, input_item_node_t * p_node, int i_pos, _Bool b_flat)'/>
<doc f='vlc/src/playlist/item.c' l='551'>/**
 * Insert a tree of input items into a given playlist node
 *
 * \param p_playlist the playlist to insert into
 * \param p_parent the receiving playlist node (can be an item)
 * \param p_node the root of input item tree,
          only it&apos;s contents will be inserted
 * \param i_pos the position in the playlist where to insert. If this is
 *        PLAYLIST_END the items will be added at the end of the playlist
 *        regardless of its size
 * \param b_flat TRUE if the new tree contents should be flattened into a list
 * \return the first new leaf inserted (in playing order)
 */</doc>
<use f='vlc/src/playlist/loadsave.c' l='120' u='c' c='input_item_subitem_tree_added'/>
