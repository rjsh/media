<dec f='include/x264.h' l='654' type='int x264_param_apply_profile(x264_param_t * , const char * profile)'/>
<use f='ffmpeg/libavcodec/libx264.c' l='730' u='c' c='X264_init'/>
<doc f='include/x264.h' l='646'>/*      (can be NULL, in which case the function will do nothing)
 *
 *      Does NOT guarantee that the given profile will be used: if the restrictions
 *      of &quot;High&quot; are applied to settings that are already Baseline-compatible, the
 *      stream will remain baseline.  In short, it does not increase settings, only
 *      decrease them.
 *
 *      returns 0 on success, negative on failure (e.g. invalid profile name). */</doc>
<use f='vlc/modules/codec/x264.c' l='1311' u='c' c='Open'/>
