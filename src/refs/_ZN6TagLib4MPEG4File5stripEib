<dec f='include/taglib/mpegfile.h' l='316' type='bool TagLib::MPEG::File::strip(int tags, bool freeMemory)'/>
<doc f='include/taglib/mpegfile.h' l='307'>/*!
       * This will strip the tags that match the OR-ed together TagTypes from the
       * file.  By default it strips all tags.  It returns true if the tags are
       * successfully stripped.
       *
       * If \a freeMemory is true the ID3 and APE tags will be deleted and
       * pointers to them will be invalidated.
       */
      // BIC: merge with the method above</doc>
