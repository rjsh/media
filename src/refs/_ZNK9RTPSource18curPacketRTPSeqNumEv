<def f='include/liveMedia/RTPSource.hh' l='79' type='u_int16_t RTPSource::curPacketRTPSeqNum() const'/>
<doc f='include/liveMedia/RTPSource.hh' l='76'>// Note that RTP receivers will usually not need to call either of the following two functions, because
  // RTP sequence numbers and timestamps are usually not useful to receivers.
  // (Our implementation of RTP reception already does all needed handling of RTP sequence numbers and timestamps.)</doc>
