<dec f='ffmpeg/libavresample/avresample.h' l='497' type='int avresample_config(AVAudioResampleContext * avr, AVFrame * out, AVFrame * in)'/>
<doc f='ffmpeg/libavresample/avresample.h' l='482'>/**
 * Configure or reconfigure the AVAudioResampleContext using the information
 * provided by the AVFrames.
 *
 * The original resampling context is reset even on failure.
 * The function calls avresample_close() internally if the context is open.
 *
 * @see avresample_open();
 * @see avresample_close();
 *
 * @param avr             audio resample context
 * @param output          output AVFrame
 * @param input           input AVFrame
 * @return                0 on success, AVERROR on failure.
 */</doc>
