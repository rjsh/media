<def f='include/UsageEnvironment/UsageEnvironment.hh' l='155' type='void TaskScheduler::turnOnBackgroundReadHandling(int socketNum, BackgroundHandlerProc * handlerProc, void * clientData)'/>
<doc f='include/UsageEnvironment/UsageEnvironment.hh' l='150'>// Causes the (previously-registered) handler function for the specified event to be handled (from the event loop).
      // The handler function is called with &quot;clientData&quot; as parameter.
      // Note: This function (unlike other library functions) may be called from an external thread - to signal an external event.

  // The following two functions are deprecated, and are provided for backwards-compatibility only:</doc>
