<dec f='include/taglib/mpcfile.h' l='182' type='APE::Tag * TagLib::MPC::File::APETag(bool create = false)'/>
<use f='vlc/modules/meta_engine/taglib.cpp' l='729' u='c' c='_ZL8ReadMetaP12vlc_object_t'/>
<use f='vlc/modules/meta_engine/taglib.cpp' l='730' u='c' c='_ZL8ReadMetaP12vlc_object_t'/>
<use f='vlc/modules/meta_engine/taglib.cpp' l='1055' u='c' c='_ZL9WriteMetaP12vlc_object_t'/>
<use f='vlc/modules/meta_engine/taglib.cpp' l='1056' u='c' c='_ZL9WriteMetaP12vlc_object_t'/>
<doc f='include/taglib/mpcfile.h' l='164'>/*!
       * Returns a pointer to the APE tag of the file.
       *
       * If \a create is false (the default) this may return a null pointer
       * if there is no valid APE tag.  If \a create is true it will create
       * an APE tag if one does not exist and returns a valid pointer.  If 
       * there already be an ID3v1 tag, the new APE tag will be placed before it.
       *
       * \note This may return a valid pointer regardless of whether or not the 
       * file on disk has an APE tag.  Use hasAPETag() to check if the file 
       * on disk actually has an APE tag.
       *
       * \note The Tag &lt;b&gt;is still&lt;/b&gt; owned by the MPEG::File and should not be
       * deleted by the user.  It will be deleted when the file (object) is
       * destroyed.
       *
       * \see hasAPETag()
       */</doc>
