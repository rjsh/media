<dec f='include/taglib/mpegfile.h' l='305' type='bool TagLib::MPEG::File::strip(int tags = AllTags)'/>
<doc f='include/taglib/mpegfile.h' l='295'>/*!
       * This will strip the tags that match the OR-ed together TagTypes from the
       * file.  By default it strips all tags.  It returns true if the tags are
       * successfully stripped.
       *
       * This is equivalent to strip(tags, true)
       *
       * \note This will also invalidate pointers to the ID3 and APE tags
       * as their memory will be freed.
       */</doc>
