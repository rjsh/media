<dec f='ffmpeg/libswscale/swscale_internal.h' l='353' type='int'/>
<offset>18944</offset>
<doc f='ffmpeg/libswscale/swscale_internal.h' l='343'>/**
     * @name Scaled horizontal lines ring buffer.
     * The horizontal scaler keeps just enough scaled lines in a ring buffer
     * so they may be passed to the vertical scaler. The pointers to the
     * allocated buffers for each line are duplicated in sequence in the ring
     * buffer to simplify indexing and avoid wrapping around between lines
     * inside the vertical scaler code. The wrapping is done before the
     * vertical scaler is called.
     */
    //@{</doc>
<doc f='ffmpeg/libswscale/swscale_internal.h' l='353'>///&lt; Last scaled horizontal luma/alpha line from source in the ring buffer.</doc>
<use f='ffmpeg/libswscale/swscale.c' l='265' u='r' c='swscale'/>
<use f='ffmpeg/libswscale/swscale.c' l='528' u='w' c='swscale'/>
