<dec f='ffmpeg/libavfilter/framepool.h' l='46' type='FFVideoFramePool * ff_video_frame_pool_init(AVBufferRef *(*)(int) alloc, int width, int height, enum AVPixelFormat format, int align)'/>
<doc f='ffmpeg/libavfilter/framepool.h' l='34'>/**
 * Allocate and initialize a video frame pool.
 *
 * @param alloc a function that will be used to allocate new frame buffers when
 * the pool is empty. May be NULL, then the default allocator will be used
 * (av_buffer_alloc()).
 * @param width width of each frame in this pool
 * @param height height of each frame in this pool
 * @param format format of each frame in this pool
 * @param align buffers alignement of each frame in this pool
 * @return newly created video frame pool on success, NULL on error.
 */</doc>
<def f='ffmpeg/libavfilter/framepool.c' l='40' type='FFVideoFramePool * ff_video_frame_pool_init(AVBufferRef *(*)(int) alloc, int width, int height, enum AVPixelFormat format, int align)'/>
<use f='ffmpeg/libavfilter/video.c' l='51' u='c' c='ff_default_get_video_buffer'/>
<use f='ffmpeg/libavfilter/video.c' l='66' u='c' c='ff_default_get_video_buffer'/>
