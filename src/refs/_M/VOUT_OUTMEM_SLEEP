<dec f='vlc/include/vlc_config.h' l='104'/>
<doc f='vlc/include/vlc_config.h' l='100'>/* Time to sleep when waiting for a buffer (from vout or the video fifo).
 * It should be approximately the time needed to perform a complete picture
 * loop. Since it only happens when the video heap is full, it does not need
 * to be too low, even if it blocks the decoder. */</doc>
<use f='vlc/modules/visualization/visual/visual.c' l='344' u='c'/>
<use f='vlc/src/input/decoder.c' l='2189' u='c'/>
