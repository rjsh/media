<dec f='ffmpeg/libavcodec/mlp.h' l='144' type='uint8_t ff_mlp_checksum8(const uint8_t * buf, unsigned int buf_size)'/>
<def f='ffmpeg/libavcodec/mlp.c' l='94' type='uint8_t ff_mlp_checksum8(const uint8_t * buf, unsigned int buf_size)'/>
<doc f='ffmpeg/libavcodec/mlp.h' l='139'>/** MLP uses checksums that seem to be based on the standard CRC algorithm, but
 *  are not (in implementation terms, the table lookup and XOR are reversed).
 *  We can implement this behavior using a standard av_crc on all but the
 *  last element, then XOR that with the last element.
 */</doc>
<use f='ffmpeg/libavcodec/mlpdec.c' l='1262' u='c' c='read_access_unit'/>
<use f='ffmpeg/libavcodec/mlpenc.c' l='1124' u='c' c='write_substrs'/>
