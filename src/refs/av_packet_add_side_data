<dec f='ffmpeg/libavcodec/avcodec.h' l='4498' type='int av_packet_add_side_data(AVPacket * pkt, enum AVPacketSideDataType type, uint8_t * data, size_t size)'/>
<doc f='ffmpeg/libavcodec/avcodec.h' l='4485'>/**
 * Wrap an existing array as a packet side data.
 *
 * @param pkt packet
 * @param type side information type
 * @param data the side data array. It must be allocated with the av_malloc()
 *             family of functions. The ownership of the data is transferred to
 *             pkt.
 * @param size side information size
 * @return a non-negative number on success, a negative AVERROR code on
 *         failure. On failure, the packet is unchanged and the data remains
 *         owned by the caller.
 */</doc>
<def f='ffmpeg/libavcodec/avpacket.c' l='295' type='int av_packet_add_side_data(AVPacket * pkt, enum AVPacketSideDataType type, uint8_t * data, size_t size)'/>
<use f='ffmpeg/libavcodec/avpacket.c' l='329' u='c' c='av_packet_new_side_data'/>
<use f='ffmpeg/libavcodec/mpegvideo_enc.c' l='2054' u='c' c='ff_mpv_encode_picture'/>
