<dec f='include/liveMedia/AC3AudioStreamFramer.hh' l='37' type='unsigned int AC3AudioStreamFramer::samplingRate()'/>
<doc f='include/liveMedia/AC3AudioStreamFramer.hh' l='33'>// If &quot;streamCode&quot; != 0, then we assume that there&apos;s a 1-byte code at the beginning of each chunk of data that we read from
  // our source.  If that code is not the value we want, we discard the chunk of data.
  // However, if &quot;streamCode&quot; == 0 (the default), then we don&apos;t expect this 1-byte code.</doc>
