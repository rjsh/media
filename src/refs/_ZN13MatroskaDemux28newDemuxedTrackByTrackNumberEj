<dec f='include/liveMedia/MatroskaFile.hh' l='135' type='FramedSource * MatroskaDemux::newDemuxedTrackByTrackNumber(unsigned int trackNumber)'/>
<doc f='include/liveMedia/MatroskaFile.hh' l='130'>// Returns a new stream (&quot;FramedSource&quot; subclass) that represents the next preferred media
      // track (video, audio, subtitle - in that order) from the file. (Preferred media tracks
      // are based on the file&apos;s language preference.)
      // This function returns NULL when no more media tracks exist.</doc>
