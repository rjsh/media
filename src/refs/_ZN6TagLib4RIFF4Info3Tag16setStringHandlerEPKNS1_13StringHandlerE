<dec f='include/taglib/infotag.h' l='173' type='static void TagLib::RIFF::Info::Tag::setStringHandler(const TagLib::RIFF::Info::StringHandler * handler)'/>
<doc f='include/taglib/infotag.h' l='162'>/*!
       * Sets the string handler that decides how the text data will be
       * converted to and from binary data.
       * If the parameter \a handler is null, the previous handler is
       * released and default UTF-8 handler is restored.
       *
       * \note The caller is responsible for deleting the previous handler
       * as needed after it is released.
       *
       * \see StringHandler
       */</doc>
