<dec f='ffmpeg/libavcodec/mediacodec.h' l='86' type='int av_mediacodec_release_buffer(AVMediaCodecBuffer * buffer, int render)'/>
<def f='ffmpeg/libavcodec/mediacodec.c' l='120' type='int av_mediacodec_release_buffer(AVMediaCodecBuffer * buffer, int render)'/>
<doc f='ffmpeg/libavcodec/mediacodec.h' l='75'>/**
 * Release a MediaCodec buffer and render it to the surface that is associated
 * with the decoder. This function should only be called once on a given
 * buffer, once released the underlying buffer returns to the codec, thus
 * subsequent calls to this function will have no effect.
 *
 * @param buffer the buffer to render
 * @param render 1 to release and render the buffer to the surface or 0 to
 * discard the buffer
 * @return 0 on success, &lt; 0 otherwise
 */</doc>
