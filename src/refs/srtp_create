<dec f='vlc/modules/access/rtp/srtp.h' l='63' type='srtp_session_t * srtp_create(int encr, int auth, unsigned int tag_len, int prf, unsigned int flags)'/>
<use f='vlc/modules/access/rtp/rtp.c' l='283' u='c' c='Open'/>
<def f='vlc/modules/access/rtp/srtp.c' l='134' type='srtp_session_t * srtp_create(int encr, int auth, unsigned int tag_len, int prf, unsigned int flags)'/>
<doc f='vlc/modules/access/rtp/srtp.c' l='120'>/**
 * Allocates a Secure RTP one-way session.
 * The same session cannot be used both ways because this would confuse
 * internal cryptographic counters; it is however of course feasible to open
 * multiple simultaneous sessions with the same master key.
 *
 * @param encr encryption algorithm number
 * @param auth authentication algortihm number
 * @param tag_len authentication tag byte length (NOT including RCC)
 * @param flags OR&apos;ed optional flags.
 *
 * @return NULL in case of error
 */</doc>
<use f='vlc/modules/stream_out/rtp.c' l='1029' u='c' c='Add'/>
