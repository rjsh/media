<def f='include/liveMedia/MultiFramedRTPSink.hh' l='77' type='Boolean MultiFramedRTPSink::isFirstPacket() const'/>
<doc f='include/liveMedia/MultiFramedRTPSink.hh' l='71'>// returns the number of overflow bytes that would be produced by adding a new
      // frame of size &quot;newFrameSize&quot; to the current RTP packet.
      // (By default, this just calls &quot;numOverflowBytes()&quot;, but subclasses can redefine
      // this to (e.g.) impose a granularity upon RTP payload fragments.)

  // Functions that might be called by doSpecialFrameHandling(), or other subclass virtual functions:</doc>
