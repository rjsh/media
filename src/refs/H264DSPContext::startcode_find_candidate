<dec f='ffmpeg/libavcodec/h264dsp.h' l='117' type='int (*)(const uint8_t *, int)'/>
<offset>2112</offset>
<doc f='ffmpeg/libavcodec/h264dsp.h' l='110'>/**
     * Search buf from the start for up to size bytes. Return the index
     * of a zero byte, or &gt;= size if not found. Ideally, use lookahead
     * to filter out any zero bytes that are known to not be followed by
     * one or more further zero bytes and a one byte. Better still, filter
     * out any bytes that form the trailing_zero_8bits syntax element too.
     */</doc>
<use f='ffmpeg/libavcodec/h264_parser.c' l='99' u='r' c='h264_find_frame_end'/>
<use f='ffmpeg/libavcodec/h264dsp.c' l='154' u='w' c='ff_h264dsp_init'/>
