<dec f='include/liveMedia/ProxyServerMediaSession.hh' l='108' type='void ProxyServerMediaSession::~ProxyServerMediaSession()'/>
<doc f='include/liveMedia/ProxyServerMediaSession.hh' l='103'>// Hack: &quot;tunnelOverHTTPPortNum&quot; == 0xFFFF (i.e., all-ones) means: Stream RTP/RTCP-over-TCP, but *not* using HTTP
      // &quot;verbosityLevel&quot; == 1 means display basic proxy setup info; &quot;verbosityLevel&quot; == 2 means display RTSP client protocol also.
      // If &quot;socketNumToServer&quot; is &gt;= 0, then it is the socket number of an already-existing TCP connection to the server.
      //      (In this case, &quot;inputStreamURL&quot; must point to the socket&apos;s endpoint, so that it can be accessed via the socket.)</doc>
