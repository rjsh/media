<def f='include/liveMedia/MediaSession.hh' l='204' type='void MediaSubsession::receiveRawMP3ADUs()'/>
<doc f='include/liveMedia/MediaSession.hh' l='198'>// Sets the preferred client port number that any &quot;RTPSource&quot; for
      // this subsession would use.  (By default, the client port number
      // is gotten from the original SDP description, or - if the SDP
      // description does not specfy a client port number - an ephemeral
      // (even) port number is chosen.)  This routine must *not* be
      // called after initiate().</doc>
<doc f='include/liveMedia/MediaSession.hh' l='204'>// optional hack for audio/MPA-ROBUST; must not be called after Initiate()</doc>
