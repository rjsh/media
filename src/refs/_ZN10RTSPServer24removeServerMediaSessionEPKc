<dec f='include/liveMedia/RTSPServer.hh' l='89' type='void RTSPServer::removeServerMediaSession(const char * streamName)'/>
<doc f='include/liveMedia/RTSPServer.hh' l='84'>// Removes the &quot;ServerMediaSession&quot; object from our lookup table, so it will no longer be accessible by new RTSP clients.
      // (However, any *existing* RTSP client sessions that use this &quot;ServerMediaSession&quot; object will continue streaming.
      //  The &quot;ServerMediaSession&quot; object will not get deleted until all of these RTSP client sessions have closed.)
      // (To both delete the &quot;ServerMediaSession&quot; object *and* close all RTSP client sessions that use it,
      //  call &quot;deleteServerMediaSession(serverMediaSession)&quot; instead.)</doc>
