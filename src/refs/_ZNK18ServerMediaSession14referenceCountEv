<def f='include/liveMedia/ServerMediaSession.hh' l='69' type='unsigned int ServerMediaSession::referenceCount() const'/>
<doc f='include/liveMedia/ServerMediaSession.hh' l='65'>// a result == 0 means an unbounded session (the default)
    // a result &lt; 0 means: subsession durations differ; the result is -(the largest).
    // a result &gt; 0 means: this is the duration of a bounded session</doc>
